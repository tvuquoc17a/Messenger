<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="userSingleton"
            type="com.example.messenger.singleton.UserSingleton" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Toolbar
            android:id="@+id/header_toolbar"
            android:layout_width="match_parent"
            android:layout_height="65dp"
            app:layout_constraintTop_toTopOf="parent">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="fill_horizontal">

                <de.hdodenhof.circleimageview.CircleImageView
                    android:id="@+id/imgHeaderUserProfile"
                    android:layout_width="0dp"
                    android:layout_height="38dp"
                    app:imageUrl="@{userSingleton.user.profileUrl}"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintDimensionRatio="1:1"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    tools:src="@drawable/market" />

                <TextView
                    android:id="@+id/tvHeaderUserName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:text="@{userSingleton.user.name}"
                    android:textColor="@color/black"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    app:layout_constraintBottom_toBottomOf="@id/imgHeaderUserProfile"
                    app:layout_constraintStart_toEndOf="@id/imgHeaderUserProfile"
                    app:layout_constraintTop_toTopOf="@id/imgHeaderUserProfile" />
            </androidx.constraintlayout.widget.ConstraintLayout>


        </Toolbar>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
